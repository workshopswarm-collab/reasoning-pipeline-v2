# Session 03/04 FIX-031/FIX-033: Canonical Machine And Sufficiency Closure

- Session: 03 Decomposer/Retrieval and 04 Researcher Verification
- Phase: Wave B fixture/blocker reconciliation
- Owner: Session 3 and Session 4, with Session 5 SCAE guard evidence
- Feature IDs: `QDT-005`, `RET-008`, `RET-009`, `CLS-006`, `CLS-008`, `CLS-005`, `CLS-007`, `VER-004`, `SCAE-013`, `MODEL-002`, `MODEL-003`, `MODEL-004`, `MIG-003`, `MIG-004`, `MIG-006`, `MIG-007`
- Migration Groups: `MIG-003`, `MIG-004`, `MIG-006`, `MIG-007`
- Fixture IDs: `FIX-031`, `FIX-032`, `FIX-033`
- Blocker IDs: `BLK-027`, `BLK-028`
- Status: `FIX-031` passing; `FIX-032` passing; `FIX-033` passing; `BLK-027` passed; `BLK-028` passed.
- Acceptance Evidence: Added `check_ads_canonical_artifacts.py` / `canonical_artifacts.py`, a deterministic static fixture scan that validates QDT, retrieval packet, leaf assignments, context-isolation audits, researcher sidecar, classification matrix, coverage bundle, and MODEL-004 model-provenance trace artifacts. The scan proves schema versions, prompt hashes, model lanes, stable IDs/digests, compact refs, and no active probability/fair-value/decision authority keys across the fixture. Added `test_thin_leaf_expands_before_research_assignment`, proving a source-of-truth thin leaf with missing source/value/negative-check coverage blocks classification dispatch, writes bounded targeted expansion attempts, refuses researcher assignment launch, then permits assignment only after expanded evidence yields a high-certainty sufficiency certificate. Added `test_skipped_evidence_or_negative_checks_fail_closed`, proving skipped assigned evidence refs and uncompleted required negative checks are rejected before coverage can be treated as SCAE-ready. Existing verification/SCAE tests prove high-certainty certificates plus complete proof yield SCAE-ready status, thin retrieval cannot become clean SCAE input, structural unanswerability requires completed confirmation, incomplete required escalations block readiness, and SCAE refuses invalid/uncertified research as production probability input.
- Checks Run: `python3 -m unittest discover -s orchestrator/scripts/tests` PASS (158 tests); `PYTHONPATH=researcher-swarm/scripts python3 -m unittest discover -s researcher-swarm/scripts/tests` PASS (135 tests); `PYTHONPATH=decomposer/scripts python3 -m unittest discover -s decomposer/scripts/tests` PASS (43 tests); `PYTHONPATH=SCAE/scripts python3 -m unittest discover -s SCAE/scripts/tests` PASS (97 tests); `python3 orchestrator/plans/check_dependency_gates.py` PASS; `python3 orchestrator/plans/check_dependency_gates.py --all --mode runtime_integration --report-only` PASS with expected Session 6 CAL blockers; `python3 -m unittest discover -s orchestrator/plans/tests` PASS (13 tests); `python3 orchestrator/scripts/bin/check_ads_canonical_artifacts.py` PASS; `python3 orchestrator/scripts/bin/check_ads_script_placement.py --report-only` completed with expected `status=failed`, `missing_path_count=30`, and `live_cutover_ready=false`; `git diff --check` PASS.
- Shared Inventory Updates Requested: none. The owned implementation rows are already `ready_for_integration`.
- Shared Map/Matrix Updates Requested: Mark `FIX-031`, `FIX-032`, and `FIX-033` `passing`; mark `BLK-027` and `BLK-028` `passed`; add the canonical scan scripts to the script placement map.
- Blockers: none for these rows. `FIX-039`/`BLK-032` and `FIX-040`/`BLK-033` remain below passing due separate script-placement and continuous-loop persistence evidence debt.
- Newly Unblocked Rows: `BLK-027` and `BLK-028` are no longer live-cutover blockers.
- Commit SHA: pending at report authoring.
